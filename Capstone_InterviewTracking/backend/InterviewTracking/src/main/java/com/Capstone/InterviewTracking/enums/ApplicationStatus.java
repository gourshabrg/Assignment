package com.Capstone.InterviewTracking.enums;

/**
 * Represents the status of a candidate's job application.
 */
public enum ApplicationStatus {

    /** Candidate has applied and is in the hiring pipeline. */
    APPLIED,

    /** Candidate has been selected after completing all rounds. */
    SELECTED,

    /** Candidate's application has been rejected. */
    REJECTED
}
